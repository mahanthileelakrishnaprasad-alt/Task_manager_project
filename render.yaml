services:
  - type: web
    name: personalhub
    runtime: python
    buildCommand: "./build.sh"
    startCommand: "gunicorn taskmaster.wsgi:application"
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: "False"
      - key: PYTHON_VERSION
        value: "3.11.0"
